import sys

# array={}
input=open(sys.argv[1])
# print(input)
for line in input:
        cleanedLine = line.strip()
        if cleanedLine: # is not empty
            print(cleanedLine)
array={}
array['id']=sorted(cleanedLine[''])
print(array)
# with open(example.txt, 'w') as fp:
#         print(cleanedLine)
